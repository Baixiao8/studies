# ── OpenAI ────────────────────────────────────────────────
# 复制本文件为 .env(同目录),把下面一行换成你自己的真实 key。
# .env 已在 .gitignore —— 不会被提交、不会外泄、不会进对话。
# 获取 key: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-在这里填你自己的key
